import { Link, useNavigate } from "react-router-dom"; import Navbar from "../components/Navbar"; import Footer from "../components/Footer"; import ProductCard from "../components/ProductCard"; import { imageOf } from "../data/catalog"; import { useStore } from "../context/StoreContext";
const money=n=>`₹${n.toLocaleString("en-IN")}`;
export function Cart(){const {cart,updateQuantity,removeFromCart}=useStore();const total=cart.reduce((s,x)=>s+x.price*x.quantity,0);return <><Navbar/><main className="container-custom narrow py-12"><p className="eyebrow">Your selection</p><h1 className="section-title">Shopping bag</h1>{!cart.length?<Empty title="Your bag is waiting" text="Add a style you love and it will appear here."/>:<><div className="space-y-4 mt-8">{cart.map(x=><div className="cart-row" key={x._id}><img src={imageOf(x)} alt=""/><div className="flex-1"><b>{x.name}</b><p className="text-sm text-stone-500">{x.fabric}</p><b className="text-[#7A1F3D]">{money(x.price)}</b></div><div className="quantity"><button onClick={()=>updateQuantity(x._id,x.quantity-1)}>−</button><span>{x.quantity}</span><button onClick={()=>updateQuantity(x._id,x.quantity+1)}>+</button></div><button className="text-sm underline" onClick={()=>removeFromCart(x._id)}>Remove</button></div>)}</div><div className="checkout-summary"><span>Total <small>Inclusive of taxes</small></span><b>{money(total)}</b><Link to="/checkout" className="button-primary">Proceed to checkout</Link></div></>}</main><Footer/></>}
export function Wishlist(){const {wishlist}=useStore();return <><Navbar/><main className="container-custom py-12"><p className="eyebrow">Saved for later</p><h1 className="section-title">Wishlist</h1>{!wishlist.length?<Empty title="Your wishlist is empty" text="Save products you want to revisit."/>:<div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-6 mt-8">{wishlist.map(p=><ProductCard key={p._id} product={p}/>)}</div>}</main><Footer/></>}
export function Checkout(){const {cart,createOrder,user,setUser}=useStore();const navigate=useNavigate();const total=cart.reduce((s,x)=>s+x.price*x.quantity,0);const submit=e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));if(!user)setUser({name:data.name,email:data.email});const o=createOrder(data);navigate(`/track?order=${o.id}`)};return <><Navbar/><main className="container-custom narrow py-12"><p className="eyebrow">Almost yours</p><h1 className="section-title">Checkout</h1>{!cart.length?<Empty title="Your bag is empty" text="Choose a style before checking out."/>:<form onSubmit={submit} className="checkout-form mt-8"><div><h2 className="font-serif text-2xl">Delivery details</h2>{[["name","Full name"],["email","Email address"],["phone","Phone number"],["address","Complete address"]].map(([n,l])=><label key={n}>{l}<input required name={n} type={n==="email"?"email":"text"} defaultValue={n==="name"?user?.name:""} className="field"/></label>)}<div className="cod"><b>Cash on Delivery</b><span>Pay when your order arrives</span></div><button className="button-primary w-full">Place COD order · {money(total)}</button></div><aside className="order-box"><h3 className="font-semibold">Order summary</h3>{cart.map(x=><p key={x._id}>{x.name} × {x.quantity}<b>{money(x.price*x.quantity)}</b></p>)}<hr/><p className="total">Total <b>{money(total)}</b></p></aside></form>}</main><Footer/></>}
export function Account(){const {user,setUser,orders}=useStore();const submit=e=>{e.preventDefault();setUser(Object.fromEntries(new FormData(e.currentTarget)))};return <><Navbar/><main className="container-custom narrow py-12"><p className="eyebrow">Your Silk & Bloom</p><h1 className="section-title">{user?`Hello, ${user.name}`:"Login or sign up"}</h1>{!user?<form className="account-box" onSubmit={submit}><p>Create an account to keep your order history in one place.</p><input required name="name" className="field" placeholder="Your name"/><input required name="email" type="email" className="field" placeholder="Email address"/><button className="button-primary">Continue</button></form>:<><button className="text-sm underline mt-3" onClick={()=>setUser(null)}>Sign out</button><h2 className="font-serif text-2xl mt-10">Order history</h2>{!orders.length?<p className="text-stone-500 mt-3">No orders yet.</p>:orders.map(o=><Link to={`/track?order=${o.id}`} className="order-item" key={o.id}><span><b>{o.id}</b><small>{o.date}</small></span><span>{o.status}</span><b>{money(o.total)}</b></Link>)}</>}</main><Footer/></>}
export function Track(){const {orders}=useStore();const query=new URLSearchParams(location.search);const found=orders.find(o=>o.id===query.get("order"));return <><Navbar/><main className="container-custom narrow py-16"><p className="eyebrow">Delivery updates</p><h1 className="section-title">Track your order</h1>{found?<div className="tracking"><b>{found.id}</b><p>Placed {found.date} · {money(found.total)}</p><div className="tracking-line"><span className="done">✓<small>Order confirmed</small></span><span>2<small>Preparing your order</small></span><span>3<small>On its way</small></span><span>4<small>Delivered</small></span></div></div>:<div className="account-box"><p>Enter an order ID from your confirmation to see its status.</p><form onSubmit={e=>{e.preventDefault();location.href=`/track?order=${new FormData(e.currentTarget).get("order")}`}}><input className="field" name="order" placeholder="e.g. SB1234567"/><button className="button-primary">Track order</button></form></div>}</main><Footer/></>}
function Empty({title,text}){return <div className="empty"><h2 className="font-serif text-3xl">{title}</h2><p>{text}</p><Link to="/shop" className="button-primary">Explore collection</Link></div>}
